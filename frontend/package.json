{
  "name": "encam",
  "version": "0.1",
  "description": "Encyclopedia of the CAncer Microenvironment frontend",
  "main": "index.js",
  "repository": "git@github.com:NBISweden/encam.git",
  "author": "NBIS",
  "license": "GPL-3.0",
  "devDependencies": {
    "parcel": "^1.12.4",
    "typescript": "^3.8.2"
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "rm dist/* -rf; parcel build index.html --no-source-maps --public-url .",
    "deploy": "yarn run build && echo deploy script TODO"
  }
}
